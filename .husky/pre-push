echo "Running unit tests..."
npm run test || {
  echo "❌ Tests failed. Fix tests before pushing."
  exit 1
}

echo "Running security audit..."
npm run audit:check || {
  echo "❌ New high/critical vulnerabilities found. Fix before pushing."
  exit 1
}

echo "✅ All pre-push checks passed!"
